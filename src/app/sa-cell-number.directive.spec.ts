import { SaCellNumberDirective } from './sa-cell-number.directive';

describe('SaCellNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new SaCellNumberDirective();
    expect(directive).toBeTruthy();
  });
});
